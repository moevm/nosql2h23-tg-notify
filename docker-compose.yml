version: "3"

services:
  backend:
    container_name: tg_api
    build: .
    restart: unless-stopped
    ports:
      - ${SERVER_PORTS}
    depends_on:
      - mongodb
    env_file:
      - .env

  mongodb:
    container_name: tg_db
    image: "mongo:6-jammy"
    restart: always
    ports:
      - ${DB_PORTS}
    env_file:
      - .env
    volumes:
      - dbdata6:/data/db
          
volumes:
  dbdata6: